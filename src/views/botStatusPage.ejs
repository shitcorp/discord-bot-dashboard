<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Discord Bot &raquo; Status</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,500,700,900" rel="stylesheet">
    <link rel="stylesheet" href="https://code.getmdl.io/1.3.0/material.indigo-pink.min.css">
    <meta name="author" content="Julian Yaman">
    <meta name="description" content="A tool for Discord.js bot developers to manage their bot for development and final release usages.">
    <link rel="stylesheet" href="../styles/styles.css">
</head>
<body>
<div class="mdl-layout mdl-js-layout mdl-layout--fixed-header">
    <main class="mdl-layout__content">
        <!-- page content -->
        <div class="page-content">
            <div class="mdl-grid">
                <div class="mdl-cell mdl-cell--3-col"></div>
                <div class="mdl-cell mdl-cell--6-col">
                    <h1><b>Status Page</b></h1>
                    <h4><i>This will be later the public status page.</i></h4>
                    <hr>
                    <p><i>Currently I´m looking for a something to provide a non-database (no MySQL and no MongoDB storage system) solution to save data into to be able to giving other users data about specific things.
                            Now, I´m giving data about the bot status and some other details that you see what I´ve planned for later... (maybe it will become later a public analytics page but you can decide if this will be public or not)</i></p>
                    <hr>
                        <% if(botData.maintenance === true){ %>
                        <div class="statusPage_botStatus botStatusDND"> Currently the bot is under maintenance!</div>
                        <% }else if(data.user.localPresence.status === "online"){ %>
                        <div class="statusPage_botStatus botStatusOnline"> All systems are working! </div>
                        <% }else if(data.user.localPresence.status === "idle"){ %>
                        <div class="statusPage_botStatus botStatusOnline"> All systems are working! </div>
                        <% }else if(data.user.localPresence.status === "dnd"){ %>
                        <div class="statusPage_botStatus botStatusOnline"> All systems are working!</div>
                        <% }else if(data.user.localPresence.status === "invisible"){ %>
                        <div class="statusPage_botStatus botStatusInvisible"> Invisible (Bot is invisible in the member list)</div>
                        <% }%>
                    <div class="statusPageSection statusPage_botGuildsInformation">
                        <h4><b>Bot in online in this guilds:</b></h4>
                        <%  let guilds = data.guilds;
                        guilds.map(function (a) {
                        %>
                        <p>&raquo; <b><%= a.name %></b></p>
                        <%
                        })
                        %>
                    </div>
                    <div class="statusPageSection statusPage_botInformation">
                        <h4><img src="<%= data.user.avatarURL %>" width="64px" class="statusPageBotInforamtion_avatar"> Bot: <b><%= data.user.username %></b></h4>
                        <h5><span class="bot_status"><b>Status:</b>
                            <% if(data.user.localPresence.status === "online"){ %>
                            <div class="statusSymbolStatusPage statusSymbolOnline"></div> Online
                            <% }else if(data.user.localPresence.status === "idle"){ %>
                            <div class="statusSymbolStatusPage statusSymbolIdle"></div> Idle
                            <% }else if(data.user.localPresence.status === "dnd"){ %>
                            <div class="statusSymbolStatusPage statusSymbolDND"></div> Do Not Disturb
                            <% }else if(data.user.localPresence.status === "invisible"){ %>
                            <div class="statusSymbolStatusPage statusSymbolInvisible"></div> Invisible
                            <% }%>
                        </span></h5>
                        <h5><b>Game:</b> <%= data.user.localPresence.game.name %></h5>
                        <h5><b>Up since:</b><br> <%= data.readyAt %></h5>
                        <h5><b>Version:</b> <%= botData.bot_version %></h5>
                    </div>
                </div>
                <div class="mdl-cell mdl-cell--3-col">
                </div>
            </div>
        </div>
    </main>
</div>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="../scripts/statusPage.js"></script>
<script defer src="https://code.getmdl.io/1.3.0/material.min.js"></script>
</body>
</html>
